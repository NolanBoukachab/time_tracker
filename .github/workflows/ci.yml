name: 'CI'
on: [pull_request]

permissions:
  contents: read

jobs:
  dependency-review:
  permissions: write-all
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python pip install pipenv
        python pipenv install safety
        python safety check