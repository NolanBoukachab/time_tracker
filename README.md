Récupéré le dépot git

Si vous souhaité l'environnement en local 
Installer l'environent avec pipenv :

```
pip install pipenv

pipenv install --dev

pipenv shell 
```

execute le script shell : 

```
bash automatic_deployment.sh
```
